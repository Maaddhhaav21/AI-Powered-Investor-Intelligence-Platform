import { PageHeader } from "@/components/PageHeader";
import { ReportViewer } from "@/components/ReportViewer";
import { ErrorState } from "@/components/ErrorState";
import { useFinancialAnalysis } from "@/hooks/useAnalysis";

export function FinancialAnalysis() {
  const { data, isLoading, isError, refetch } = useFinancialAnalysis();

  return (
    <div>
      <PageHeader
        eyebrow="AI Generated"
        title="Financial Analysis"
        description="Generated from the latest uploaded report"
      />

      {isError ? (
        <ErrorState onRetry={() => refetch()} />
      ) : (
        <ReportViewer
          title="Financial Analysis"
          markdown={data?.answer}
          isLoading={isLoading}
          onRegenerate={() => refetch()}
          badge={{
            label: "Generated by AI",
            variant: "default",
          }}
        />
      )}
    </div>
  );
}
