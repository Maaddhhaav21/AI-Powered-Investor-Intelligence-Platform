# Point this at your backend once it's live; falls back to in-app mock data otherwise.
VITE_API_BASE_URL=/api

# Set to "false" to call the real backend endpoints instead of bundled demo data.
VITE_USE_MOCKS=true
